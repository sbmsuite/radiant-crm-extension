#empty
  == You don't have any #{@view} tasks.
  - if @view != "completed"
    == Feel free to #{link_to_remote("create a new task", :url => new_admin_task_path + "?view=#{@view}", :method => :get)}.
