- title = (recent.subject.respond_to?(:full_name) ? recent.subject.full_name : recent.info)
%li{ :class => recent != @recent.last ? "" : "last" }
  %dt= link_to(truncate(title, :length => 19), polymorphic_url([:admin, recent.subject]), :title => title)
  %tt= recent.subject_type + ":"
