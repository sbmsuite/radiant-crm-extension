%p
  = link_to_close options_admin_opportunities_path

%p

%form
  = hidden_field_tag "opportunity[user_id]", @current_user.id
  = render :partial => "sort_by"
  = render :partial => "admin/common/per_page"
  = render :partial => "admin/common/outline"
  = submit_to_remote(:save, "Save", :url => "#{redraw_admin_opportunities_url}", :loading => "$('loading').show()", :complete => "$('loading').hide()")
%br
