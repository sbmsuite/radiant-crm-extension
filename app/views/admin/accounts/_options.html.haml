%p
  = link_to_close options_admin_accounts_path

%p

%form
  = hidden_field_tag "account[user_id]", @current_user.id
  = render :partial => "sort_by"
  = render :partial => "admin/common/per_page"
  = render :partial => "admin/common/outline"
  = submit_to_remote(:save, "Save", :url => "#{redraw_admin_accounts_url}", :loading => "$('loading').show()", :complete => "$('loading').hide()")
%br
